# ============================================
# 🔒 PRODUCTION ENVIRONMENT VARIABLES
# ============================================
# Ove varijable se trebaju postaviti u Vercel Project Settings
# Verzija: Vercel 2.0+
#
# Kako postaviti:
# 1. Idi na https://vercel.com/dashboard
# 2. Izaberi projekt
# 3. Settings → Environment Variables
# 4. Dodaj sledeće varijable sa production vrednostima
#
# ============================================

# Database - KORISTI PRODUCTION PostgreSQL
# Ovo bi trebalo biti tvoja production Postgres baza
# Format: postgresql://[username]:[password]@[host]:[port]/[database]?schema=public
DATABASE_URL="postgresql://prod_user:prod_password@prod.db.example.com:5432/mvp_usluge?schema=public"

# NextAuth - KORISTI PRODUCTION SECRET
# Generiši novi secret za production:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
NEXTAUTH_SECRET="your-production-secret-here-32-random-chars"

# NextAuth - KORISTI PRODUCTION URL
# Ovo je URL gde je aplikacija deployovana
NEXTAUTH_URL="https://tvoj-domen.com"

# Email - KORISTI PRODUCTION EMAIL API
# Za Resend ili bilo koji drugi email servis
RESEND_API_KEY="re_actual_production_key"
RESEND_FROM_EMAIL="noreply@tvoj-domen.com"

# Cron - KORISTI PRODUCTION SECRET
# Isti secret kao u lokalnom .env (ili drugačiji ako želiš)
CRON_SECRET="production-cron-secret-change-this"

# Redis - KORISTI PRODUCTION UPSTASH
# Ako koristiš Upstash Redis
UPSTASH_REDIS_REST_URL="https://your-production-upstash-endpoint.upstash.io"
UPSTASH_REDIS_REST_TOKEN="your-production-upstash-token"

# ============================================
# DODATNE PRODUCTION VARIJABLE (ako koristiš)
# ============================================

# NODE_ENV - OBAVEZNO "production"
NODE_ENV="production"

# VERCEL_ENV - Automatski postavljen od Vercel-a
# VERCEL_ENV="production"

# ============================================
# NAPOMENA
# ============================================
# - Nikada ne pushuj prave varijable u git! Koristi Vercel UI
# - Ovaj fajl je samo template sa примерима
# - Za svaku production varijablu koristi različitu vrednost nego što imaš lokalno
# - Pazi na security! Ne deli production secrets
